import React, { Component } from "react";

class Login extends Component {
  render() {
    return (
      <div>
        login pg - check if person is logged in or not. if yes, allow use of site. if no, ask user to log in or register.
      </div>
    );
  }
}

export default Login;
